import PageShell from "@/components/PageShell";
import { Calendar } from "lucide-react";

export default function Planner() {
  return <PageShell title="Planner" description="Organize your schedule" icon={Calendar} />;
}
